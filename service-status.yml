---
- hosts: node1
  tasks:
  - name: Nginx Service Statusss
    command: systemctl status nginx.service
    register: service_status

 - name: Nginx_Service_Statusss
   debug:
     msg: "{{service_status.stdout_lines}}"
